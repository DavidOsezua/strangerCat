import React from "react";
import "../App.css";

const Transaction = () => {
  return (
    <section className={`section `}>
      <div className={`sectionContianer py-0`}>Transactions</div>
    </section>
  );
};

export default Transaction;
